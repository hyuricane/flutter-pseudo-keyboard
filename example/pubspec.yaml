name: example
description: Demonstrates how to use the pseudo_keyboard package.

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  pseudo_keyboard:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true